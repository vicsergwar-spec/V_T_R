# V_T_R - Video Transcriptor y Resumen
# Dependencias de Python

# Framework web
flask>=3.0.0
flask-cors>=4.0.0

# Google Gemini API
google-generativeai>=0.7.0  # 0.7+ incluye la API de Context Caching

# OpenAI (opcional, para Whisper API como respaldo)
openai>=1.12.0

# Whisper para transcripción local (motor optimizado con CTranslate2)
# Nota: Instalar con pip install faster-whisper
faster-whisper>=1.0.0

# PyTorch con CUDA - Instalar manualmente con:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# torch

# Procesamiento de audio
pydub>=0.25.1

# Variables de entorno
python-dotenv>=1.0.0

# Utilidades
werkzeug>=3.0.0

# Procesamiento de imágenes (para SlideExtractor)
Pillow>=10.2.0

# Generación de PDF para descarga de slides
fpdf2>=2.7.9

# Ventana nativa de escritorio (sin necesidad de Chrome/Edge)
pywebview>=4.4.1
